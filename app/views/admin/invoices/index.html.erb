<%= render partial: 'shared/admin_dashboard_nav' %>

<div class="row">
  <div class="col-sm-12">
    <p class='col-head'>
      Invoices
    </p>
  </div>
</div>

<% @invoices.each do |i| %>
  <li>
    <%= link_to "Invoice ##{i.id}", admin_invoice_path(i) %>
  </li>
<% end %>
