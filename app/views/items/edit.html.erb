<%= render partial: 'shared/merchant_dashboard_nav', locals: { merchant: @merchant } %>

<div class='row'>
  <% # FULL COLUMN %>
  <div class='col-sm-12'>
    <p class='col-head'>
      Update Item
    </p>
  </div>

  <%= render partial: 'form', locals: { method: :patch,
                                        url: merchant_item_path(@merchant, @item),
                                        class: 'merchant_item_form',
                                        local: true,
                                        model: @item } %>

</div>
