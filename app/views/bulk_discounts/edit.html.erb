<%= render partial: 'shared/merchant_dashboard_nav', locals: { merchant: @merchant } %>

<%= render partial: 'form', locals: { model: [@merchant, @bulk_discount],
                                      button_text: 'Update' } %>
